$NetBSD$

TODO list for NetBSD/aarch64

- known bugs
  - __thread variables doesn't work (ld.elf_so)
  - C++ stack unwinding (libunwind?)

- not yet
  - SMP
  - kernel text/rodata segments are mapped writable. it shoule be mapped as RO when no options DDB.
  - ucas_ras_check() is not implemented yet
  - ddb disassembler
  - crash(8), savecore(8) and libkvm (cpu_kcore_hdr_t)
  - pmap should be work even if PID_MAX >= 65536
  - TLB ASID in pmap shoule be randomize
  - COMPAT_NETBSD32
